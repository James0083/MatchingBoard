<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.multi.mapper.RoomMapper">
	
	<insert id="insertRoom" parameterType="Room">
    	insert into room(roomid, rname, rplace, rmaxpeople, rgenre, rgame, rstr, rdatetime)
    	values (#{roomid}, #{rname}, #{rplace}, #{rmaxpeople}, #{rgenre}, #{rgame}, #{rstr}, #{rdatetime})
	</insert>
	
	<select id="selectRoomByIdx" resultType="Room" parameterType="string">
		select * from room where roomid=#{value}
	</select>
	
</mapper>